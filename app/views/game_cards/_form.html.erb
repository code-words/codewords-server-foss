<%= form_with(model: game_card, local: true) do |form| %>
  <% if game_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_card.errors.count, "error") %> prohibited this game_card from being saved:</h2>

      <ul>
      <% game_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :card_id %>
    <%= form.text_field :card_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.number_field :address %>
  </div>

  <div class="field">
    <%= form.label :chosen %>
    <%= form.check_box :chosen %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
