language: ruby
cache: bundler
before_install:
  - gem update --system
  - gem install bundler
services:
- postgresql
before_script:
- cp config/database.yml.travis config/database.yml
- psql -c 'create database travis_ci_test;' -U postgres
